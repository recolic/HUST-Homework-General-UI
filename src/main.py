#!/usr/bin/env python3

import sys

# TODO: parse config file, accept commandline arg, call parser to get funclist, call cli-app-generator to compile cmdline app.
#       translate funclist to uniform format, call ui to generate proper ui. 
#       if genexe is required and available, generate a self-extract exe(use py2exe) with all resources packed in.
